# Автоматизированное тестирование сайта AutomationExercise.com

## 📋 Описание проекта

Этот проект содержит набор автоматизированных UI-тестов для сайта [AutomationExercise.com](https://automationexercise.com/) - демонстрационного сайта для практики автоматизации тестирования.

Проект реализован с использованием:
- **Python 3.12+**
- **Selenium WebDriver** - для автоматизации браузера
- **Selenium Grid** - для запуска тестов в удаленных браузерах (Chrome и Firefox)
- **RemoteWebDriver** - для взаимодействия с удаленными браузерами по сети
- **Docker Compose** - для управления контейнерами Selenium Grid
- **pytest** - фреймворк для тестирования
- **Page Object Pattern** - паттерн проектирования для организации кода

**Особенности:**
- ✅ Тесты автоматически запускаются в двух браузерах (Chrome и Firefox)
- ✅ Используется Selenium Grid для распределенного выполнения тестов
- ✅ Браузеры запускаются в Docker-контейнерах (не требуют локальной установки)

## 🏗️ Структура проекта

```
Lab9/
├── config/                      # Конфигурационные файлы с тестовыми данными
│   ├── test_login.json         # Данные для тестов авторизации
│   ├── test_addToCart.json     # Данные для тестов добавления в корзину
│   ├── test_makeOrder.json     # Данные для тестов оформления заказа
│   └── test_search.json        # Данные для тестов поиска
│
├── pages/                       # Page Object классы (паттерн Page Object)
│   ├── login_page.py           # Класс для работы со страницей входа
│   ├── cart_page.py            # Класс для работы с корзиной
│   ├── order_page.py           # Класс для работы с оформлением заказа
│   ├── search_page.py          # Класс для работы с поиском
│   └── category_page.py        # Класс для работы с категориями
│
├── tests/                       # Тестовые сценарии
│   ├── test_login.py           # Тесты авторизации
│   ├── test_addToCart.py       # Тесты добавления в корзину
│   ├── test_makeOrder.py       # Тесты оформления заказа
│   └── test_search.py          # Тесты поиска
│
├── conftest.py                 # Конфигурация pytest и фикстуры (RemoteWebDriver)
├── docker-compose.yml          # Конфигурация Selenium Grid (Hub, Chrome, Firefox)
├── requirements.txt            # Зависимости проекта
├── README.md                   # Этот файл
└── REGISTRATION_GUIDE.md       # Руководство по регистрации
```

## 🎯 Реализованные тесты

Проект содержит **4 основных приемочных теста**:

### 1. **Авторизация** (`test_login.py`)
- ✅ Успешная авторизация с валидными данными
- ✅ Авторизация с неверными данными (проверка обработки ошибок)

### 2. **Добавление товара в корзину** (`test_addToCart.py`)
- ✅ Добавление одного товара в корзину
- ✅ Добавление нескольких товаров в корзину

### 3. **Оформление заказа** (`test_makeOrder.py`)
- ✅ Оформление заказа авторизованным пользователем

### 4. **Поиск товаров** (`test_search.py`)
- ✅ Поиск товаров по запросу
- ✅ Поиск с пустым запросом

## 🔧 Установка и настройка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Установка Docker и Docker Compose

Для работы с Selenium Grid необходимо установить Docker и Docker Compose:

- **Docker Desktop** (Windows/Mac): https://www.docker.com/products/docker-desktop
- **Docker Engine** (Linux): https://docs.docker.com/engine/install/

**Примечание**: Локальная установка ChromeDriver или FirefoxDriver не требуется, так как браузеры запускаются в Docker-контейнерах через Selenium Grid.

### 3. Настройка тестовых данных

**ВАЖНО**: Перед запуском тестов необходимо зарегистрировать пользователя на сайте и обновить конфигурационные файлы.

1. Откройте https://automationexercise.com/
2. Зарегистрируйте нового пользователя (см. `REGISTRATION_GUIDE.md`)
3. Обновите `config/test_login.json`:
```json
{
  "valid_credentials": {
    "email": "ваш_email@example.com",
    "password": "ваш_пароль"
  }
}
```
4. Обновите `config/test_makeOrder.json` с теми же данными

## 🚀 Запуск тестов

### Важно: Настройка Selenium Grid

Проект использует **Selenium Grid** для запуска тестов в удаленных браузерах (Chrome и Firefox).

#### 1. Запуск Selenium Grid через Docker Compose

Перед запуском тестов необходимо запустить Selenium Grid:

```bash
docker-compose up -d
```

Эта команда запустит:
- **Selenium Hub** на порту 4444
- **Chrome Node** - нода для запуска тестов в Chrome
- **Firefox Node** - нода для запуска тестов в Firefox

#### 2. Проверка работы Selenium Grid

Откройте в браузере: http://localhost:4444

Вы должны увидеть веб-интерфейс Selenium Grid с информацией о доступных нодах.

#### 3. Остановка Selenium Grid

После завершения тестов можно остановить контейнеры:

```bash
docker-compose down
```

### Запуск тестов

**ВАЖНО**: Убедитесь, что Selenium Grid запущен перед запуском тестов!

#### Запуск всех тестов (в обоих браузерах)

Тесты автоматически запускаются в Chrome и Firefox благодаря параметризации фикстуры:

```bash
pytest tests/ -v
```

Каждый тест будет выполнен дважды - один раз в Chrome и один раз в Firefox.

#### Запуск конкретного теста

```bash
pytest tests/test_login.py -v
```

#### Запуск с подробным выводом

```bash
pytest tests/ -v -s
```

#### Запуск с HTML отчетом

```bash
pytest tests/ --html=report.html
```

#### Запуск тестов только в одном браузере

Если нужно запустить тесты только в Chrome:

```bash
pytest tests/ -v -k "chrome"
```

Или только в Firefox:

```bash
pytest tests/ -v -k "firefox"
```

**Примечание**: Из-за параметризации фикстуры, каждый тест будет запущен в обоих браузерах по умолчанию.

## 📚 Архитектура проекта

### Паттерн Page Object Model (POM)

Проект использует паттерн **Page Object Model**, который разделяет код на три уровня:

#### 1. **Конфигурационные файлы** (`config/`)
- Содержат все входные данные и ожидаемые результаты
- Формат: JSON
- Преимущества:
  - Легко изменять данные без изменения кода
  - Централизованное хранение тестовых данных
  - Возможность использования разных наборов данных

#### 2. **Page Object классы** (`pages/`)
- Содержат методы для взаимодействия с элементами страницы
- Инкапсулируют логику работы с веб-элементами
- Преимущества:
  - Переиспользование кода
  - Легкая поддержка при изменении структуры страницы
  - Чистый и читаемый код

**Пример структуры Page Object класса:**
```python
class LoginPage:
    # Локаторы элементов (CSS селекторы, XPath)
    EMAIL_INPUT = (By.CSS_SELECTOR, "input[data-qa='login-email']")
    
    def __init__(self, driver):
        self.driver = driver
    
    def login(self, email, password):
        # Метод для выполнения действия
        pass
```

#### 3. **Тестовые сценарии** (`tests/`)
- Содержат сами тесты с проверками (assertions)
- Используют методы из Page Object классов
- Преимущества:
  - Читаемые тесты, похожие на документацию
  - Легко добавлять новые тесты
  - Разделение логики и проверок

**Пример структуры теста:**
```python
def test_login(driver):
    # 1. Загружаем данные
    data = load_test_data()
    
    # 2. Создаем Page Object
    page = LoginPage(driver)
    
    # 3. Выполняем действия
    page.open(data["url"])
    page.login(data["email"], data["password"])
    
    # 4. Проверяем результат
    assert page.is_logged_in()
```

## 🔍 Разбор кода

### 1. conftest.py - Конфигурация pytest

```python
@pytest.fixture(scope="function", params=["chrome", "firefox"])
def driver(request):
    """
    Фикстура создает RemoteWebDriver для работы с Selenium Grid.
    Scope "function" означает создание нового драйвера для каждого теста.
    params=["chrome", "firefox"] обеспечивает запуск каждого теста в обоих браузерах.
    """
    browser = request.param
    if browser == "chrome":
        options = ChromeOptions()
        driver = webdriver.Remote(
            command_executor="http://localhost:4444/wd/hub",
            options=options
        )
    elif browser == "firefox":
        options = FirefoxOptions()
        driver = webdriver.Remote(
            command_executor="http://localhost:4444/wd/hub",
            options=options
        )
    driver.maximize_window()
    yield driver  # Передача управления тесту
    driver.quit()  # Выполнится после теста
```

**Что такое фикстура?**
- Фикстура - это функция, которая выполняется до и после теста
- `@pytest.fixture` - декоратор для создания фикстуры
- `scope="function"` - драйвер создается для каждого теста отдельно
- `yield` - разделяет код "до" и "после" теста

### 2. Page Object классы

#### LoginPage - Работа со страницей входа

**Основные компоненты:**

1. **Локаторы элементов** - CSS селекторы и XPath для поиска элементов:
```python
EMAIL_INPUT = (By.CSS_SELECTOR, "input[data-qa='login-email']")
LOGIN_BUTTON = (By.CSS_SELECTOR, "button[data-qa='login-button']")
```

2. **Методы взаимодействия:**
   - `open(base_url)` - открывает страницу
   - `open_login_page()` - переходит на страницу входа
   - `login(email, password)` - выполняет авторизацию
   - `get_success_message()` - получает сообщение об успешном входе
   - `is_logged_in()` - проверяет, авторизован ли пользователь

**Особенности реализации:**
- Используется `WebDriverWait` для ожидания элементов
- Обработка исключений для надежности
- Несколько способов поиска элементов (fallback механизм)

#### CartPage - Работа с корзиной

**Ключевые методы:**

1. **add_product_to_cart_by_name(product_name)**
   - Находит товар по названию
   - Использует ActionChains для наведения мыши
   - JavaScript клик для надежности

2. **go_to_cart()**
   - Использует JavaScript клик для обхода перекрытия модальным окном
   - Обрабатывает различные состояния модального окна

3. **continue_shopping()**
   - Закрывает модальное окно
   - Ожидает его полного закрытия

#### OrderPage - Оформление заказа

**Процесс оформления заказа:**
1. `proceed_to_checkout()` - переход к оформлению
2. `place_order()` - размещение заказа
3. `fill_payment_form(card_data)` - заполнение данных карты
4. `submit_payment()` - подтверждение платежа
5. `get_success_message()` - получение подтверждения

#### SearchPage - Поиск товаров

**Особенности:**
- Автоматический переход на страницу Products, если поле поиска не найдено
- Поддержка пустого запроса
- Подсчет результатов поиска

### 3. Тестовые сценарии

#### Структура теста

Каждый тест следует структуре **AAA (Arrange-Act-Assert)**:

1. **Arrange (Подготовка)**
   ```python
   data = load_test_data()
   page = LoginPage(driver)
   ```

2. **Act (Действие)**
   ```python
   page.open(data["url"])
   page.login(data["email"], data["password"])
   ```

3. **Assert (Проверка)**
   ```python
   assert page.is_logged_in()
   ```

#### Загрузка данных

```python
def load_test_data():
    with open("config/test_login.json", encoding='utf-8') as f:
        return json.load(f)
```

**Важно:** Используется `encoding='utf-8'` для корректной работы с кириллицей в комментариях JSON.

## 🛠️ Используемые технологии и паттерны

### Selenium WebDriver
- **WebDriverWait** - явные ожидания элементов
- **Expected Conditions** - условия для ожидания
- **ActionChains** - действия с мышью (наведение, клик)
- **JavaScript execution** - выполнение JS для надежности кликов

### Паттерны проектирования

1. **Page Object Model (POM)**
   - Инкапсуляция логики работы со страницами
   - Переиспользование кода
   - Легкая поддержка

2. **Data-Driven Testing**
   - Тестовые данные в отдельных файлах
   - Легко создавать новые тестовые наборы

3. **Fixture Pattern (pytest)**
   - Переиспользование настроек
   - Изоляция тестов

## 📝 Локаторы элементов

### Типы локаторов

1. **CSS селекторы** - быстрые и читаемые
   ```python
   EMAIL_INPUT = (By.CSS_SELECTOR, "input[data-qa='login-email']")
   ```

2. **XPath** - более гибкие, но медленнее
   ```python
   SUCCESS_MESSAGE = (By.XPATH, "//a[contains(@href, '/logout')]")
   ```

### Стратегии поиска элементов

1. **Приоритет атрибутов:**
   - `data-qa` - специальные атрибуты для тестирования
   - `id` - уникальные идентификаторы
   - `class` - классы элементов
   - `name` - имена полей форм

2. **Fallback механизм:**
   - Несколько вариантов локаторов
   - Альтернативные способы поиска
   - Обработка исключений

## ⚠️ Важные замечания

### 1. Учетные данные
- **Обязательно** зарегистрируйте пользователя перед запуском тестов
- Используйте уникальный email для избежания конфликтов
- Храните реальные данные только в локальных конфигах (не коммитьте в Git)

### 2. Стабильность тестов
- Используются явные ожидания (`WebDriverWait`)
- JavaScript клики для обхода проблем с перекрытием элементов
- Обработка исключений для надежности

### 3. Производительность
- Каждый тест создает новый браузер (изоляция)
- Можно оптимизировать через `scope="session"` для группы тестов
- Используются таймауты для избежания бесконечных ожиданий

## 🐛 Отладка тестов

### 1. Запуск с отладкой
```bash
pytest tests/ -v -s  # -s показывает print statements
```

### 2. Остановка на ошибке
```bash
pytest tests/ --pdb  # Запускает отладчик при ошибке
```

### 3. Скриншоты при ошибках
Можно добавить в `conftest.py`:
```python
@pytest.hookimpl(tryfirst=True, hookwrapper=True)
def pytest_runtest_makereport(item, call):
    outcome = yield
    rep = outcome.get_result()
    if rep.when == "call" and rep.failed:
        driver = item.funcargs.get("driver")
        if driver:
            driver.save_screenshot(f"screenshot_{item.name}.png")
```

## 📊 Результаты тестирования

После успешного запуска вы увидите:
```
7 passed in 89.08s
```

Все тесты должны проходить успешно при правильной настройке данных.

## 🔗 Полезные ссылки

- [Selenium Documentation](https://www.selenium.dev/documentation/)
- [pytest Documentation](https://docs.pytest.org/)
- [Page Object Pattern](https://www.selenium.dev/documentation/test_practices/encouraged/page_object_models/)
- [AutomationExercise.com](https://automationexercise.com/)

## 💡 Детальный разбор кода

### Архитектура проекта

Проект следует принципам **чистой архитектуры** и **разделения ответственности**:

```
┌─────────────────────────────────────────────────────────┐
│                    ТЕСТОВЫЙ СЛОЙ                         │
│  (tests/) - Тесты с проверками (assertions)            │
└──────────────────┬──────────────────────────────────────┘
                   │ использует
┌──────────────────▼──────────────────────────────────────┐
│              PAGE OBJECT СЛОЙ                            │
│  (pages/) - Методы взаимодействия с элементами          │
└──────────────────┬──────────────────────────────────────┘
                   │ использует
┌──────────────────▼──────────────────────────────────────┐
│            КОНФИГУРАЦИОННЫЙ СЛОЙ                         │
│  (config/) - Тестовые данные (JSON)                    │
└──────────────────────────────────────────────────────────┘
```

### Принципы работы

#### 1. Фикстуры pytest (conftest.py)

```python
@pytest.fixture(scope="function", params=["chrome", "firefox"])
def driver(request):
    """
    Фикстура создает RemoteWebDriver для работы с Selenium Grid.
    
    Преимущества scope="function":
    - Каждый тест начинается с чистого состояния
    - Тесты не влияют друг на друга
    - Легче отлаживать отдельные тесты
    
    Преимущества params=["chrome", "firefox"]:
    - Каждый тест автоматически запускается в обоих браузерах
    - Обеспечивает кросс-браузерное тестирование
    """
    browser = request.param
    if browser == "chrome":
        options = ChromeOptions()
        driver = webdriver.Remote(
            command_executor="http://localhost:4444/wd/hub",
            options=options
        )
    elif browser == "firefox":
        options = FirefoxOptions()
        driver = webdriver.Remote(
            command_executor="http://localhost:4444/wd/hub",
            options=options
        )
    driver.maximize_window()
    yield driver  # Передача управления тесту
    driver.quit()  # Очистка после теста
```

**Как это работает:**
- `@pytest.fixture` - декоратор для создания фикстуры
- `scope="function"` - создается для каждого теста
- `params=["chrome", "firefox"]` - параметризация: каждый тест запускается дважды
- `request.param` - получает текущее значение параметра (chrome или firefox)
- `webdriver.Remote()` - создает удаленный драйвер, подключенный к Selenium Grid Hub
- `yield` - разделяет код "до" и "после" теста
- Тест получает драйвер через параметр `driver`

#### 2. Page Object Pattern

**Пример структуры Page Object:**

```python
class LoginPage:
    # ЛОКАТОРЫ - селекторы для поиска элементов
    EMAIL_INPUT = (By.CSS_SELECTOR, "input[data-qa='login-email']")
    
    def __init__(self, driver):
        self.driver = driver  # Сохраняем драйвер для использования
    
    # МЕТОДЫ - действия с элементами
    def login(self, email, password):
        # Логика взаимодействия с элементами
        pass
    
    # МЕТОДЫ ПРОВЕРКИ - получение состояния
    def is_logged_in(self):
        # Проверка состояния
        return True/False
```

**Преимущества:**
- **Инкапсуляция**: Вся логика работы со страницей в одном месте
- **Переиспользование**: Методы можно использовать в разных тестах
- **Поддержка**: При изменении страницы меняем только Page Object
- **Читаемость**: Тесты читаются как документация

#### 3. Явные ожидания (WebDriverWait)

```python
# ПЛОХО: Неявное ожидание (может быть нестабильным)
element = driver.find_element(By.ID, "button")
element.click()  # Может упасть, если элемент еще не загрузился

# ХОРОШО: Явное ожидание (надежнее)
element = WebDriverWait(driver, 10).until(
    EC.element_to_be_clickable((By.ID, "button"))
)
element.click()  # Гарантированно кликабельный элемент
```

**Типы ожиданий:**
- `presence_of_element_located` - элемент присутствует в DOM
- `visibility_of_element_located` - элемент видим на странице
- `element_to_be_clickable` - элемент кликабелен

#### 4. Стратегии поиска элементов

**Fallback механизм** - несколько способов поиска:

```python
def get_success_message(self):
    try:
        # Стратегия 1: Основной способ
        element = driver.find_element(By.ID, "success")
    except:
        try:
            # Стратегия 2: Альтернативный способ
            element = driver.find_element(By.CSS_SELECTOR, ".success")
        except:
            # Стратегия 3: Последняя попытка
            element = driver.find_element(By.XPATH, "//div[@class='success']")
    return element.text
```

**Почему это важно:**
- Структура сайта может изменяться
- Разные браузеры могут по-разному рендерить элементы
- Повышает надежность тестов

#### 5. JavaScript клики

```python
# Обычный клик (может не сработать, если элемент перекрыт)
button.click()

# JavaScript клик (всегда сработает)
driver.execute_script("arguments[0].click();", button)
```

**Когда использовать:**
- Элемент перекрыт другим элементом (модальное окно)
- Обычный клик не срабатывает
- Нужна гарантированная работа

#### 6. ActionChains для действий с мышью

```python
from selenium.webdriver.common.action_chains import ActionChains

actions = ActionChains(driver)
actions.move_to_element(product).perform()  # Наведение мыши
```

**Использование:**
- Наведение мыши на элемент (для появления hover-меню)
- Перетаскивание элементов
- Комбинации клавиш

### Структура теста (AAA Pattern)

Каждый тест следует паттерну **Arrange-Act-Assert**:

```python
def test_login(driver):
    # ========== ARRANGE (Подготовка) ==========
    data = load_test_data()  # Загружаем данные
    page = LoginPage(driver)  # Создаем Page Object
    
    # ========== ACT (Действие) ==========
    page.open(data["url"])  # Открываем страницу
    page.login(data["email"], data["password"])  # Выполняем действие
    
    # ========== ASSERT (Проверка) ==========
    assert page.is_logged_in()  # Проверяем результат
```

**Преимущества:**
- Четкая структура
- Легко понять, что делает тест
- Легко найти ошибку (в каком этапе)

### Работа с данными

**Загрузка из JSON:**

```python
def load_test_data():
    with open("config/test_login.json", encoding='utf-8') as f:
        return json.load(f)
```

**Важно:** `encoding='utf-8'` необходим для корректной работы с кириллицей в комментариях JSON.

**Структура JSON:**

```json
{
  "url": "https://automationexercise.com/",
  "valid_credentials": {
    "email": "user@example.com",
    "password": "password123"
  },
  "expected_success_message": "Logged in as"
}
```

### Обработка ошибок

**Try-Except блоки:**

```python
def get_success_message(self):
    try:
        # Пытаемся найти элемент
        element = WebDriverWait(driver, 10).until(...)
        return element.text
    except:
        # Если не нашли, возвращаем None
        return None
```

**Почему это важно:**
- Тесты не падают на неожиданных ошибках
- Можно использовать fallback стратегии
- Более информативные сообщения об ошибках

## 📄 Лицензия

Этот проект создан в учебных целях для практики автоматизации тестирования.

## 👤 Автор

Проект создан для лабораторной работы по курсу "Автоматизация тестирования".

---

**Примечание:** Для получения дополнительной информации о регистрации на сайте см. файл `REGISTRATION_GUIDE.md`.

